array.LASTINDEXOF()
// Searches an array for the last occurence of the element,and returns the Index numbers
    

let mobile = ['iPhone', 'Samsung', 'Xiaomi', 'iPhone', 'Tecno', 'Itel', 'Infinix', 'iPhone'];
let result = mobile.lastIndexOf('iPhone');
console.log(result);
//Index of the last iPhone is 7, so it returns 7 as the answer

